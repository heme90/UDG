// Dependencies
var restful = require('node-restful');
var mongoose = restful.mongoose;

// Schema
var generoSchema = new mongoose.Schema({
	genero: String
});

// Return model
module.exports = restful.model('Generos', generoSchema);